This directory contains configuration files for the Crawler.

Structure

    Tokens/
        PublicTokens/
            ACCESS_TOKEN
            CONSUMER
        SecretTokens/
            ACCESS_TOKEN
            CONSUMER
    LOCATIONS

Description

    Tokens/PublicTokens/ACCESS_TOKEN
        Every line contains a public access token.

    Tokens/PublicTokens/CONSUMER
        Every line contains a public consumer token.

    Tokens/SecretTokens/ACCESS_TOKEN
        Every line contains a secret access token.

    Tokens/PublicTokens/CONSUMER
        Every line contains a secret consumer token.

    LOCATIONS
        Every line contains
            -Location name
            -Longitude
            -Latitude
            -Search radius (in kilometers)
            -WOEID